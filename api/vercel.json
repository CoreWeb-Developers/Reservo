{
  "version": 2,
  "builds": [
    {
      "src": "api/server/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/server/$1" },
    { "src": "/admin/(.*)", "dest": "admin/$1" },
    { "src": "/(.*)", "dest": "client/$1" }
  ]
}